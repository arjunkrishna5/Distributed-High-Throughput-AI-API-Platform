# Spring Boot application configuration file specifying web server port and settings.
# Configures Gateway to run on HTTP port 8080 with Swagger UI enabled.

server:
  port: 8080

spring:
  application:
    name: gateway-service

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
